apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresql-wrapper.fullname" . }}-test-connection"
  labels:
    {{- include "postgresql-wrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgresql-wrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
