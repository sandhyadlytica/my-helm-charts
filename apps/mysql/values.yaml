mysql:
  auth:
    rootPassword: rootpass123
    username: demo
    password: demopass123
    database: demo_db

  primary:
    service:
      type: ClusterIP
    persistence:
      enabled: true
      size: 8Gi

  metrics:
    enabled: true

# 👇 Required by test-connection template
service:
  port: 3306

# 👇 Required by serviceaccount.yaml template
serviceAccount:
  create: true

ingress:
  enabled: false
